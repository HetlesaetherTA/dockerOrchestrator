```.env
${APP_PATH}/blueprints
```

Is where docker-compose.yml files managed by this application is stored, the `blueprint` type is a superset of <a href="https://github.com/compose-spec/compose-spec/tree/main">compose-spec</a> that support dockerOrchestrator specific attributes. `compose-spec` is the schema definition implemented by docker-compose. 
